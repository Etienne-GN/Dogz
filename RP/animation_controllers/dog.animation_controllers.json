{
	"format_version": "1.10.0",
	"animation_controllers": {
		"controller.animation.dog.move": {
			"states": {
				"default": {
					"animations": [
						{ "look": "query.is_alive" },
						{ "walk": "query.modified_move_speed > 0.1 && !query.is_baby && !query.is_sheared" },
						{ "sit": "query.is_baby && !query.is_sheared" },
						{ "lay_down": "query.is_sheared && query.is_baby" }
					],
					"transitions": [
						{ "downed": "query.health <= 3 && query.is_tamed" }
					]
				},
				"downed": {
					"animations": [ "lay_down" ],
					"transitions": [
						{ "default": "query.health > 3" }
					]
				}
			}
		}
	}
}
